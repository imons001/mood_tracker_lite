name: mood_tracker_lite
description: "A simple Flutter app for tracking moods 🌿"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_dotenv: ^5.2.1
  intl: ^0.19.0
  shared_preferences: ^2.3.1
  carousel_slider: ^5.1.1
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/neutraljin.png
    - assets/images/forestneutral.png
    - .env
